import 'package:flutter/material.dart';

class TvBottomControl extends StatelessWidget {
  const TvBottomControl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("TV Bottom Control"),
    );
  }
}
